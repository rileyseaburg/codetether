# Legacy Python Agent Worker

This package is deprecated and retained for compatibility/reference only.

Use the Rust worker in `codetether-agent/` for active deployments:

```bash
codetether worker --server https://api.codetether.run --codebases /path/to/project --auto-approve safe
```

The legacy installer and systemd units remain here so older deployments can be migrated deliberately.
