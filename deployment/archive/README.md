# Archived deployment assets

Historical manifests, kubeconfigs, and deployment helper scripts preserved from earlier deployment approaches. Active production deployment should use `chart/`, `docker/`, `scripts/`, GitHub Actions, and the Makefile targets.

Before using anything in this directory, audit it for stale endpoints, credentials, namespaces, and image names.
