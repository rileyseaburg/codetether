import { Badge } from './Badge'
import { JsonCounts } from './JsonCounts'
import { formatDate } from '../utils'
import type { WorkflowSummary } from '../types'

export function WorkflowsTable({ rows, loading }: { rows: WorkflowSummary[]; loading: boolean }) {
  return <section className="rounded-2xl border border-slate-200 bg-white shadow-sm"><div className="border-b border-slate-200 p-5"><h2 className="text-lg font-semibold text-slate-950">Workflow groups</h2><p className="mt-1 text-sm text-slate-500">Grouped by repository and GitHub issue/PR identity.</p></div><div className="overflow-x-auto"><table className="min-w-full divide-y divide-slate-200 text-sm"><thead className="bg-slate-50 text-left text-xs font-semibold uppercase tracking-wide text-slate-500"><tr><th className="px-4 py-3">Workflow</th><th className="px-4 py-3">Status</th><th className="px-4 py-3">Routing</th><th className="px-4 py-3">Agents</th><th className="px-4 py-3">Last update</th><th className="px-4 py-3">Failure</th></tr></thead><tbody className="divide-y divide-slate-100">{rows.map((w, i) => <tr key={`${w.repo}-${w.issue_pr}-${w.url}-${i}`} className="align-top"><td className="px-4 py-4"><div className="font-semibold text-slate-900">{w.repo}</div><div className="text-xs text-slate-500">#{w.issue_pr || 'unlinked'}</div>{w.url ? <a className="text-xs text-cyan-700 hover:underline" href={w.url} target="_blank" rel="noreferrer">Open GitHub</a> : null}</td><td className="px-4 py-4"><JsonCounts counts={w.status_counts} /></td><td className="px-4 py-4 text-xs text-slate-600"><div>unscoped: {w.unscoped_pending_count}</div><div>stale: {w.stale_pending_count}</div></td><td className="px-4 py-4"><JsonCounts counts={w.agent_counts} /></td><td className="px-4 py-4 text-xs text-slate-600">{formatDate(w.last_update)}</td><td className="px-4 py-4"><Badge value={w.error_class} />{w.errors ? <div className="mt-2 max-w-sm truncate text-xs text-slate-500" title={w.errors}>{w.errors}</div> : null}</td></tr>)}{!loading && !rows.length ? <tr><td colSpan={6} className="px-4 py-8 text-center text-slate-500">No active GitHub App workflow groups found.</td></tr> : null}</tbody></table></div></section>
}
