# Database assets

Database-level assets that are not part of the main application migration package.

Primary application migrations live in `a2a_server/migrations/`. Files here should be reviewed and either promoted into that migration stream or kept as historical/manual database scripts.
