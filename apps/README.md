# Apps

Product-facing applications and services that are part of the CodeTether ecosystem but are not the core Python API server.

- `voice-agent/` — LiveKit/Qwen voice agent service and fine-tuning helpers.
